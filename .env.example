ONYX_API_KEY=
ONYX_API_BASE=
LIBRECHAT_MONGODB="mongodb://mongodb.librechat.local:27017/"

# When running the importer from outside Docker, point these to a hostname/IP
# that actually reaches the containers (e.g., host.docker.internal on macOS/Windows
# or the server's LAN address). Avoid using plain "localhost" unless the script
# itself executes inside the Docker network.
POSTGRES_HOST=relational-db.onyx.local
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=postgres

S3_ENDPOINT_URL=http://minio.onyx.local:9000
S3_AWS_ACCESS_KEY_ID=minioadmin
S3_AWS_SECRET_ACCESS_KEY=minioadmin
S3_FILE_STORE_BUCKET_NAME=onyx-file-store-bucket
