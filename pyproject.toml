[tool.ruff]
target-version = "py311"
line-length = 100
exclude = [
    "__pycache__",
    "exports",
    "*.jsonl",
]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.ruff.lint.isort]
known-first-party = ["lc_mig"]
